# Training (run on the rented GPU box)
torch>=2.3.0
transformers>=4.51.0
peft>=0.11.0
trl>=0.9.0
bitsandbytes>=0.43.0
accelerate>=0.30.0
datasets>=2.19.0

# Generation pipeline (existing, run anywhere)
google-genai
python-dotenv
